apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-dep
  labels:
    app: dev
    env: {{ .Values.env }}

spec:
  replicas: 5

  selector:
    matchLabels:
      app: dev
      env: {{ .Values.env }}

  template:
    metadata:
      labels:
        app: dev
        env: {{ .Values.env }}

    spec:
      containers:
        - name: nginx
          image: babugyadav/nginx:{{ .Values.image.tag }}
          ports:
            - containerPort: 80